<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:layout_gravity="center"
    tools:context=".BeaconFinderActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_above="@+id/beacon_range_text"
        android:text="@string/beacon_range"
        android:textColor="#ff000000"
        android:textSize="30sp" />

    <TextView
        android:id="@+id/beacon_range_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_centerHorizontal="true"
        android:text="@string/beacon_not_found"
        android:textColor="#ffff0010"
        android:textSize="40sp" />

    <RelativeLayout
        android:layout_below="@id/beacon_range_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true">
    <TextView
        android:id="@+id/beeping_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/beeping_text"
        android:textColor="#ff000000"
        android:textSize="15sp" />

    <Switch
        android:id="@+id/beeping_switch"
        android:layout_alignBottom="@id/beeping_text_view"
        android:layout_toRightOf="@id/beeping_text_view"
        android:layout_toEndOf="@id/beeping_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="5sp"/>
</RelativeLayout>

</RelativeLayout>
